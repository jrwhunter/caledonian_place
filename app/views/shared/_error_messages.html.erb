<% if @suggestion.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@suggestion.errors.count, "error") %>.
    </div>
    <ul>
    <% @suggestion.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
